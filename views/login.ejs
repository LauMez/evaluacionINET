<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <!-- BOOTSTRAP -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.3/font/bootstrap-icons.css">
    <link rel="stylesheet" href="resources/css/login.css">
</head>
<body>
    <div class="main__container">
        <form action="/auth" method="POST" class="login__container">
            <div class="login__volver">
                <a href="/"><i class="bi bi-arrow-left"></i></a>
            </div>
            <div class="login__ss">
                <img src="resources/img/logo.jpg" class="login__ss-img">
                <p class="login__ss-title"><span>¡H</span>ola de nuevo<span>!</span></p>
                <p class="login__ss-text"><span>I</span>ngresa tus credenciales para acceder a tu cuenta<span>.</span></p>
            </div>
            <div class="login__info">
                <input type="email" name="email" class="mb2" placeholder="Correo"> <br>
                <input type="password" name="password" placeholder="Contraseña"> 
            </div>
            <br> 
            <div class="login__btn">
                <input type="submit" class="login__btn-btn" value="Login">
                <p class="login__btn-register"><span>¿T</span>odavia no tenés una cuenta<span>?</span><a href="register"> Registrate aquí</a></p>
            </div>
        </form>
    </div>

    <script src="resources/scripts/toggle.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>

    <% if(typeof alert != "undefined") { %>
        <script>
            Swal.fire({
                title: '<%= alertTitle %>',
                text: '<%= alertMessage %>',
                icon: '<%= alertIcon %>',
                showConfirmButton: false,
                timer: 1500
           }).then(()=> {
                window.location='/<%= ruta %>'
           })
        </script>
    <% } %>
</body>
</html>